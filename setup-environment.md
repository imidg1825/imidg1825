# ⚡ Как развернуть окружение за 5 минут

Эта инструкция описывает, как я настраиваю рабочее окружение  
для **Python / QA Automation / API / Docker**.

Используется связка **Windows + WSL (Ubuntu) + VS Code** — стабильная и удобная
для повседневной разработки и тестирования.

---

## 🖥 Поддерживаемая среда

- Windows 10 / 11  
- WSL2 + Ubuntu  
- Visual Studio Code  
- Python 3.10+  

---

## 1️⃣ Установка WSL (Ubuntu)

Если WSL ещё не установлен:

```bash
wsl --install
После установки:

перезагрузи компьютер

запусти Ubuntu

задай имя пользователя и пароль

📌 Вся дальнейшая работа ведётся внутри Ubuntu (WSL).

2️⃣ Установка VS Code и подключение к WSL

Установи Visual Studio Code

Установи расширение Remote - WSL

В VS Code нажми:

Ctrl + Shift + P

Remote-WSL: Connect to WSL

После этого VS Code будет работать напрямую с Linux-окружением.

3️⃣ Установка расширений VS Code

Минимальный набор для работы:

Python — запуск и отладка кода

Pylance — анализ типов и автодополнение

Ruff — линтер и автофикс (ручной запуск)

Black Formatter — форматирование Python-кода

Docker — работа с контейнерами

GitLens / Git Graph — удобная работа с Git

📄 Полный список расширений:
👉 extensions-list.md

4️⃣ Установка Python-зависимостей

В терминале проекта (WSL):
sudo apt update
sudo apt install -y python3-pip
Установка зависимостей проекта:
pip install -r requirements.txt
5️⃣ Проверка окружения

Пример быстрой проверки:
python3 app.py
Если код запускается без ошибок — окружение настроено корректно ✅
🧠 Принципы настройки
форматирование кода — автоматически (Black)
линтинг и автофиксы — осознанно (Ruff вручную)
среда максимально приближена к реальной рабочей

📌 Инструкция используется для:
быстрого развёртывания окружения на новом ПК
воспроизводимости проектов
демонстрации подхода к рабочей среде
